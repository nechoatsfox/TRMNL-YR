<div class="content content--center">
  <span class="value value--xlarge">{{ current_temp }}°</span>
  <span class="label">{{ current_condition }}</span>
  <span class="label">{{ location_name }}</span>
</div>
